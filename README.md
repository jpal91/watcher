# Watcher

A file watcher command line utility that executes commands on changes. This project has many similarities to other projects of the same type but the main differences are - 

1. Works off of a global configuration file instead of a local one or having to add the file(s) by command
2. Utilizes a definied debounce before executing the command

This is a WIP, not suitable for usage yet.
